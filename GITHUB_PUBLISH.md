# GitHub Publish Status / GitHub 发布状态

Published repository:

已发布仓库：

https://github.com/Edward-May7/ce-zlsma-xauusd

## Status / 状态

- Repository owner / 仓库所有者: `Edward-May7`
- Repository name / 仓库名: `ce-zlsma-xauusd`
- Visibility / 可见性: Public / 公开
- Default branch / 默认分支: `main`
- Baseline dataset / 基线数据: `data/XAUUSD_M5_202505010100_202604302255.csv`
- Baseline report / 基线报告: `TEST_REPORT.md`

## Notes / 说明

The local `git push` path may fail in this environment because direct Git HTTPS access to `github.com` can time out. The repository was published through the GitHub REST Git data API using the authenticated Git Credential Manager account.

当前环境中直接通过 Git HTTPS 访问 `github.com` 可能超时，因此本仓库通过已认证的 Git Credential Manager 账号和 GitHub REST Git data API 发布。

For future local pushes, use a network path that can reach `github.com` over HTTPS, or keep using GitHub API based publishing.

后续如需本地 `git push`，请使用可以访问 `github.com` HTTPS 的网络环境，或继续使用 GitHub API 方式发布。
